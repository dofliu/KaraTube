# 更新日誌 (Changelog)

本檔記錄每個發布版本的變更。格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，
版本號遵循 [語意化版本](https://semver.org/lang/zh-TW/)。

逐次迭代的開發細節（為什麼這樣做、踩到什麼）記在 [docs/PROGRESS_LOG.md](docs/PROGRESS_LOG.md)。

> 版本號的唯一真相來源是 `backend/version.py`。發布流程會檢查 git 標籤、
> `backend/version.py` 與本檔最上面那一筆三者一致，對不上就不給發。

## [1.18.0] - 2026-09-20

### 新增

- **自動接歌 (Auto DJ)**：沒有人點歌時，機器自己從**已經備好的曲庫**接一首上來。
  商用點歌機沒有「安靜」這個狀態，而包廂裡真正被那段安靜傷到的是氣氛 ——
  一首唱完之後那三十秒沒有聲音，所有人會同時低頭滑手機。預設**關著**
  （會讓機器自己發出聲音的規則，包廂要先講好才開），在
  「⚙️ 系統設定 → 🎧 自動接歌」打開。
  - **功能本身三行就寫得完，難的全部在「機器什麼時候該閉嘴」**。一台會自己
    放歌的機器，只要挑錯時機就會變成包廂裡最吵的那個東西，所以這個功能真正的
    內容是一串「不要在這時候出聲」的規則。
  - **只從快取曲庫挑，絕不去 YouTube 抓新歌**。抓一首要下載＋人聲分離＋
    歌詞對齊，是幾分鐘的 GPU 工作 —— 而觸發它的時機正好是「大家在聊天、
    等下一首」，等於趁包廂安靜時把運算資源吃光，然後真的有人點歌時那一首跑不動。
  - **空了要先等一下下才接**（預設 20 秒）。切歌之後那幾秒鐘多半有人正握著
    手機在找下一首，機器搶著放歌的話，使用者的下一個動作是按切歌 ——
    一個需要人動手關掉的貼心功能就是不貼心。而且空閒是從「這一刻沒歌可播」
    起算：佇列裡還有一首在跑流水線時不算空閒，那首跑完會自己接上。
  - **有人點歌時，看「有沒有人已經唱進去了」再決定讓不讓位**。機器接的那一首
    開播 **45 秒內就讓位**（大概還在前奏與第一段），**超過就唱完再換**。
    已經唱到一半的人被切掉，比點歌的人多等兩分鐘難堪得多，而且多等的上限是
    一首歌的長度（可預期）—— 跟「時間到不停掉正在唱的那一首」同一個原則。
  - **它必須有一個會自己停下來的理由**：連著接滿設定的首數（預設 3 首）就安靜
    下來，有人點一首就重新計數。「沒有人點歌」最常見的原因不是大家都想聽機器
    放歌，是**沒有人在了** —— 沒有這一條，一間沒有人的包廂會整夜唱到隔天早上。
  - **機器接的歌不算任何人的一首**：不進點唱排行、不進已唱歷史、不佔輪序與
    額度、也不會把包廂的錶打開。排行那一條是硬性的 —— 自動接歌照排行挑歌，
    播出來又計進排行的話，那條排行就變成機器自己的回音（熱門的被接得更多、
    被接得更多所以更熱門），三個晚上之後榜上只剩它愛放的那五首。
  - **挑歌用加權隨機，不用排序**：照次數排、挑最高的會讓同一首被接到爛，
    而包廂第二次聽到同一首就會去按切歌。四種來源（混著挑／只挑我的最愛／
    只挑常點的／只挑還沒唱過的）都是權重，熱門歌還封了頂。已經排在佇列裡的歌
    **任何情況下都不挑**（偷跑掉別人排好的歌，那個人待會會看到自己點的歌
    「已經唱過了」）；「剛剛接過的」則在小曲庫挑不到歌時才放寬，並在畫面上標出來。
  - 舞台片頭卡與點歌台都會標「🎧 自動接歌・有人點歌就讓開」——
    憑空出現一首沒有人點的歌，不講的話看起來像機器壞了（或像誰偷點的）。

- **🎲 來一首（隨機點歌）**：佇列上方新增一顆 🎲，從已備好的曲庫隨機點一首。
  「想唱歌但想不到要唱什麼」跟搜尋是兩件事 —— 搜尋要先想得出關鍵字。
  它跟自動接歌共用同一副挑歌規則（設「只挑我的最愛」時 🎲 也照辦），
  但**算人點的**：計入排行與歷史、佔輪序與額度，照樣會被歡唱時間與點歌上限
  擋下來 —— 這顆鍵是點歌的捷徑，不是繞過規則的後門。不必開自動接歌也能用。

### 變更

- 新增 `GET /api/autofill`（規則與現況）、`GET /api/autofill/preview`
  （「現在接的話會接哪一首」，只挑不播、沒有副作用）與
  `POST /api/autofill/random`（🎲）。自動接歌的狀態也跟著每一次 `STATE_UPDATE`
  走（`state["autofill"]`）：點歌台要標「現在這首是機器接的」，而那個標記
  必須跟佇列是同一份狀態，否則會出現「歌換了、標記還停在上一首」的半秒鐘。
- `🎲` 被擋下來時的判斷刻意排在挑歌**之前**：挑完再擋的話，曲庫裡剩下的歌
  剛好都排在佇列裡時，使用者收到的會是「沒有歌可以挑」，而真正的理由是
  他排太多了 —— 那兩句話的下一步完全不同（去點別的歌 vs. 等一首唱完）。
- 自動接歌掛在既有的舞台心跳迴圈上（五秒一次），跟包廂計時與訊息過期共用：
  「空了二十秒」這件事必須有人發現，而包廂最安靜的時候正是沒有人會去按
  任何按鈕的時候。
- 曲庫清單（`LibraryIndex.entries()`）在接歌前丟到執行緒裡跑，不佔住 event loop
  —— 它會掃整個快取資料夾，而呼叫它的時機正是舞台要放歌的時候。

## [1.17.0] - 2026-09-19

### 新增

- **歌星查歌：注音首字查歌星 (Artist Search)**：點歌台新增 **🎤 歌星查歌** 分頁 ——
  跟 🔤 注音查歌同一副鍵盤、不同索引：那邊按的是歌名的字，這邊按的是**歌星名字**
  的字（周杰倫＝ㄓㄐㄌ，英文名按首字母＝J C）。包廂裡更常用的其實是這一邊：
  想不起歌名的人，一定記得自己要唱誰。選一位歌星就翻開他在曲庫裡的全部歌，
  每一首都是快取秒播。
  - **一個人不該在歌星清單裡出現三次**。這是整個功能唯一難的地方。曲庫的歌手名
    是從頻道名與標題還原的，同一位歌星因此會長出「周杰倫」「Jay Chou」
    「周杰倫 Jay Chou」三種字串。照原字串建索引，清單上就會出現三個周杰倫，
    每一個手上只有他一部分的歌 —— 使用者按 ㄓㄐㄌ 進去看到兩首，會以為曲庫裡
    就只有這兩首，然後回頭去 YouTube 重下載一份已經有的歌。所以先把同一個人的
    多種寫法併成一位，漢字寫法與英文寫法都進索引：**按 ㄓㄐㄌ 或按 J C 落在
    同一位，而且是同一份完整歌單**。
  - **併要保守，寧可漏併也不要錯併**。只在「整段漢字完全相同」或「整段英文完全
    相同」時併，不做子字串比對 ——「張惠妹」與「張惠」不是同一個人。而且
    **漢字不同就是不同人**：兩位歌星的英文名剛好一樣時，漢字那一關會擋下來。
    錯併的代價是歌單裡混進別人的歌（使用者會當成系統壞了），比漏併嚴重得多；
    沒有證據說「周杰倫」與「Jay Chou」是同一個人時（曲庫裡沒有那筆混寫的），
    寧可留成兩位也不猜。
  - **按到只剩一位就直接翻開歌單**。畫面上只剩一位歌星時，再要求使用者點一下
    沒有任何資訊量。但畫面會說出「只剩這一位，直接翻開歌單」—— 不說的話，
    那個自己跳掉的畫面看起來像壞掉。
  - **別名直接印在歌星鈕上**（「周杰倫 ・ 也寫作 Jay Chou」），不是只放 hover
    的 title：包廂那台是觸控螢幕，根本沒有 hover，看不到的解釋等於沒解釋。
  - **「未知歌星」不進注音索引，但也不會消失**：它不是名字，按注音永遠不該撈到
    它；可是那些歌是真的存在的，所以照樣列在最後一位，使用者才不會納悶曲庫的
    數字為什麼對不起來。
  - 會落空的鍵一樣變灰（整片會變灰時就一個都不變灰），查不到時講得出是哪一關
    擋住的（注音字典沒裝？還是曲庫裡真的沒有這位）—— 跟注音查歌同一套規則，
    兩個鍵盤的手感必須一模一樣。

### 變更

- 新增 `GET /api/library/artists/keys`（注音鍵位、幾位歌星、幾首認不出歌星）與
  `GET /api/library/artists/find?q=&artist=&limit=`（查歌星）。後者**把歌星清單
  與那位歌星的歌單放在同一包回傳** —— 使用者要的是歌不是名字，分兩支 API 會讓
  「按到只剩一位就自動翻開歌單」多閃一次空畫面。兩支都丟到 executor 跑，
  理由跟注音查歌相同（第一次查要替整個曲庫建索引）。
- 歌星索引接在 `SongFinder` 後面而不是自己掃一次曲庫：歌星清單、歌名索引與
  分類瀏覽共用同一份資料，才不會出現「分類瀏覽看得到、歌星查歌查不到」這種
  兩套清單對不起來的狀況；歌單裡的歌名也跟著是萃出來的歌名本體。
- 歌星查歌不另外寫檔：它是從 `artist_name`（分類瀏覽早就算好並快取在
  `metadata.json` 的那一欄）即算即回的，所以刪快取就跟著消失，不留孤兒索引。

## [1.16.0] - 2026-09-18

### 新增

- **曲庫查歌：注音首字 / 歌名字數 (Bopomofo & Length Search)**：商用點歌機實體鍵盤
  上的那三條路（注音首字、歌名字數、歌手），補在點歌台的 **🔤 注音查歌** 分頁。
  跟上面的搜尋框是兩件事：搜尋框查的是 YouTube（點下去要等 AI 跑完），
  這一頁**只查已經備好的曲庫**，列出來的每一首都是快取秒播 ——
  曲庫大了之後，「這首我們是不是已經有了」比「去 YouTube 找一首」常用得多。
  功能本身聽起來就是「畫一個注音鍵盤」，難的其實是**索引要建在哪一串字上**。
  - **YouTube 標題不是歌名**。曲庫裡那首歌叫「周杰倫 Jay Chou - 稻香 Rice
    Fields【Official MV】4K」。照整串字建索引，兩種查法都當場廢掉：字數是
    二十幾個（沒有人會去按「23 個字」），注音首字從「ㄓ」開始（按「ㄉㄒ」的人
    查不到稻香）。所以 `title_core()` 先萃歌名本體 —— 括號附註、歌手（含只用
    空白隔開的）、宣傳字樣、尾巴的英譯都剝掉，`《稻香》` 這種明示歌名的優先採用，
    剝完只剩歌手時改用括號裡的（「五月天 Mayday【入陣曲】」）。
    萃不出東西時回原標題，**絕不回空字串**：曲庫裡出現一首沒有名字的歌，
    比查不到更糟。
  - **多音字存的是集合，不是一個答案**。「重」的首碼是 ㄓ 也是 ㄔ，索引每個字
    保留所有讀音，比對時落在候選裡就算命中。只留字典排第一的讀音看起來省事，
    代價是另一半使用者永遠查不到那首歌 —— 而且**他不會知道為什麼**，
    他會以為曲庫裡沒有這首，然後回頭去 YouTube 再下載一份已經有的歌。
  - **比對是連續子序列，不是只比開頭**。商用機多半要求從第一個字按起，
    但使用者記得的常常是中間那幾個字（〈甲你攬牢牢〉記得的是「攬牢牢」）。
    兩種都收，排序上讓開頭命中的排前面。
  - **會落空的鍵變灰，但不會整片變灰**。伺服器連「這一串後面還接得下哪些鍵」
    （`next_keys`）一起回傳，其餘的鍵變灰 —— 是變灰不是消失，鍵盤位置每次都
    一樣，手指才記得住。例外是**整個鍵盤都會變灰的時候一個都不變灰**
    （下一格是英文單字、或已經沒有下一格）：全灰的畫面沒給任何資訊，
    只看起來像壞掉。
  - **一個輸入框吃三種查法**。注音、字數與直接打歌名／歌手共用同一個框
    （伺服器本來就三種都吃），使用者不必想「我該打在哪一格」。
  - **查不到的時候講得出是哪個條件擋住的**：字數還開著、注音字典沒裝、
    還是真的沒有這首。空白畫面配一句「找不到」，使用者的下一個動作就是回去
    用搜尋框把已經在曲庫裡的歌再下載一次。
  - 歌卡標題顯示**萃出來的歌名本體**而不是整串 YouTube 標題：查的是「稻香」，
    列出來卻是「周杰倫 Jay Chou - 稻香…【4K】」很難掃視。

### 變更

- 新增 `GET /api/library/find/keys`（注音鍵位、字數桶、`bopomofo_available`）與
  `GET /api/library/find?q=&chars=&limit=`（查歌）。兩支都丟到 executor 跑：
  第一次查要替整個曲庫建索引，那是幾百次讀檔，不能佔住 event loop 讓別人點不了歌。
- 查歌索引（歌名本體、字數、逐字首碼）寫回每首歌 `metadata.json` 的 `find_index`，
  跟語言/歌手分類同一個做法：算一次就好，刪快取就跟著消失，不留孤兒索引。
  索引帶版本號（`INDEX_VERSION`），萃取或注音規則改了就自動重算 ——
  不然使用者會拿著新鍵盤去查用舊規則建的索引，查不到還找不出原因。
- 新增相依套件 `pypinyin`（純 Python、幾百 KB，注音首碼與多音字來源）。
  **沒裝也不會壞**：`bopomofo_available` 回 `false`，鍵盤收起來並說明原因，
  字數與打字查詢照常；之後裝上了，索引會自動補建注音那一段。
  `requirements-dev.txt` 也裝，CI 才測得到真正的比對行為而不是退化路徑。
- 新增 `tests/test_song_index.py`（31 條）與 `frontend/tests/library-search.test.js`
  （20 條）。前者釘的是「查得到什麼」，後者釘的是「查不到的那一刻畫面上寫什麼」。

## [1.15.0] - 2026-09-17

### 新增

- **舞台訊息（跑馬燈）(Stage Marquee)**：商用點歌機的「櫃檯把字打到包廂螢幕上」——
  「您的餐點到了」、生日祝福、「麥克風請傳給下一位」。點歌台（與手機）最上排的
  **📺 舞台訊息** 打一句話就送到舞台，常用句按一下就填好。
  功能本身三行就寫得完（存一段字、推出去、顯示），難的是**那段字會蓋掉
  畫面上的什麼** —— 舞台那面螢幕上已經沒有空地了，而唯一不能搶的就是歌詞：
  台上那個人正在看它唱歌。
  - **訊息只走畫面最上緣，而且是「推開」不是「蓋住」**。訊息帶是 `.stage-wrapper`
    裡的一個 flex 元素，出現時整排 HUD（歌名、歡唱倒數、分數）往下讓位，
    所以它不可能疊到任何一個字；歌詞與音準線那半邊完全不動。絕對定位的浮層
    遲早會壓到某個東西，而那件事只有在包廂裡、只有在訊息跳出來的那幾秒
    才看得到（開發時不會遇到）。
  - **大字卡只在沒有人唱歌的時候出現**。櫃檯要的是「一定看得到」，但那句話在
    副歌那一句等於「一定擋到」。所以同一則訊息有兩種身體：沒在播歌時是置中的
    大字卡，播歌中自動降級成上緣那一條。**緊急訊息也一樣降級** —— 它換到的
    優待是排最前面、停留久一點（×1.5），不包含「可以蓋住歌詞」。設定頁因此
    **沒有**「播歌時也用大字卡」這個選項。
  - **它不捲動**。叫它跑馬燈是因為大家都這樣叫它，但會動的字會一直把餘光
    拉過去，而唱歌的人正需要那個餘光看歌詞。只有一次淡入，然後停著讓人讀完。
    40 字的上限也是從這裡來的：一行、讀得完、不用捲。
  - **每一則都會自己消失**（預設 10 分鐘）。「您的餐點到了」在四十分鐘之後才
    出現在螢幕上，是比沒有訊息更糟的**錯誤資訊**。要久一點的用 **📌 釘住**，
    而釘住的天花板是 4 小時 —— 刻意沒有「永不過期」：散場之後還掛在螢幕上的
    生日祝福，是下一桌客人看到的第一個東西。
  - **一次只講一件事**：同時最多 8 則，多則輪播而不是一起掛上去
    （兩行以上的跑馬燈沒有人讀得完，而讀不完等於全都沒讀到）。
  - **訊息不落地**。計時要存檔（客人買了多久不該因為重開機而重算），訊息剛好
    相反：伺服器重開之後最可能的狀況是那件事早就處理完了，而一則沒有人記得的
    舊訊息自己跳到螢幕上，看起來就像機器壞了。
  - 面板下方列出現在螢幕上的每一則（誰送的、還剩多久），可以單獨撤掉或全部撤掉；
    「全部撤掉」與「留下釘住的」是兩顆不同的行為（後者是 `include_pinned=false`）。
    誰送的只在點歌台看得到，舞台上不顯示 —— 台下的人要看的是那句話，不是誰打的。

### 變更

- 新增 `GET /api/marquee`、`POST /api/marquee`、`DELETE /api/marquee/{id}`、
  `DELETE /api/marquee`，有變動時廣播 `MARQUEE_UPDATE`（整份狀態，含現在生效的
  規則）。規則跟著清單一起送，舞台才知道「沒在播歌時要不要用大字卡」、
  點歌台才知道「預設幾分鐘後消失」—— 分兩支端點拿的話會有一邊是舊的。
- 擋下來一律回 **409** 而不是 400（滿了、空訊息、功能關著），跟點歌額度與
  歡唱時間同一種語氣：說明規則，並且講出下一步（等一則播完、或先撤掉一則）。
  靜靜地失敗是最糟的結局 —— 送出的人會以為螢幕上已經有字了，
  然後對著客人說「您看一下螢幕」。
- 訊息的存活時間在畫面上是**相減**算出來的（快照裡的兩個伺服器時間相減 ＋
  收到之後過了多久），不比對絕對時刻：舞台那台機器的時鐘常常跟伺服器差好幾
  分鐘，比對絕對時刻的話訊息會提早消失或不肯消失。跟包廂計時同一套算法。
- 原本的包廂計時心跳（`room_timer_loop`）改名為 `stage_heartbeat_loop`，
  多一件事：丟掉過期的訊息並廣播。兩件事共用一個迴圈是因為它們要的是同一件
  事 ——「時間自己過去了」這件事必須有人發現，沒有人會為了讓一則舊訊息消失
  而去按什麼。
- 系統設定新增「📺 舞台訊息」四個欄位（開關、每則停留幾秒、幾分鐘後消失、
  沒在播歌時要不要用大字卡）。預設**開著**：它不會自己跳出來（沒有人送訊息就
  什麼都不會發生），而關著的話，需要用它的那一刻（餐點送到門口）沒有人會
  想到要先去設定頁打開。

## [1.14.0] - 2026-09-16

### 新增

- **包廂計時（歡唱時間）(Room Timer)**：商用點歌機的「歡唱時間到」。難的從來
  不是倒數 —— 倒數是減法。難的是**時間到之後怎麼辦**。商用機的做法是直接停掉
  正在唱的那一首，而那是這整件事裡最糟的一種收法：被停掉的那個人正站在包廂
  中間、麥克風在他手上、他剛唱到副歌，然後畫面黑掉。他不會覺得「時間到了」，
  他會覺得「機器在我唱到一半的時候把我關掉」。
  點歌台右上角顯示倒數，可以 **開始 / ＋30 分續時 / 暫停 / 結束**；
  系統設定頁的「⏱️ 包廂計時」預設**關著**（家裡唱歌沒有人在算時間）。
  - **時間到不停歌，停的是「下一首」**。正在唱的那一首唱完，唱完之後機器停在
    那裡不自動播下一首。超時的上限因此是一首歌的長度（三到五分鐘），是可預期的；
    相對的，停在副歌那一句是不可預期的難堪。用三分鐘換掉那個畫面很划算。
    設定頁因此**沒有**「立刻停掉這一首」這個選項 —— 那正是這個功能要消滅的畫面，
    做成一個選項等於把它留在那裡等人選到。只提供「唱完這一首再停」與
    「只提醒，不停歌」。
  - **佇列一首都不刪**。停下來的是播放，不是資料。刪掉的話續時之後大家得重點
    一輪（而且沒有人記得剛剛排了什麼）；更糟的是「時間到」與「佇列被清空」
    在畫面上是同一秒發生的，看起來就像機器當掉了。
  - **提醒要早、要少、而且要講「時間到會發生什麼事」**。預設提醒兩次
    （剩 10 分鐘、剩 3 分鐘）加上時間到那一次，每個門檻只講一次 ——
    每 30 秒跳一次的提醒會被當成雜訊直接無視，然後真正重要的那一次也一起被無視。
    每一句都會講出「時間到會讓正在唱的那一首唱完」與怎麼續時：不講的話，
    最後一首的點歌者會以為自己被偷走一首歌；講了的話，剩下的十分鐘大家會自己安排。
    一次跨過好幾個門檻時（伺服器剛重開、心跳被卡住）只講最急的那一個。
  - **續時是加時間，不是重開一場**，而且停住時續時會**自己接回去播下一首** ——
    按了「續時」還得再按一次播放的話，那顆續時鍵看起來就沒有反應。
    「結束計時」同樣會解除停播：那顆鍵的意思是「不要再管時間了」，
    按完卻還停在散場畫面不肯播的話，沒有人找得到怎麼救回來。
  - **暫停就是真的停錶**（餐點來了、中場休息）。暫停期間不計時，而且刻意不順便
    暫停播放 —— 停錶是「不要算我的時間」，跟「把歌停下來」是兩件事。
  - **第一首歌自動開錶**（可關）。要靠人記得按「開始計時」的話，最常見的結局是
    三小時後才有人想起來沒按，那時候這個功能等於沒開，而且已經補不回來了。
    按過「結束計時」之後就不再自動開錶。
  - **計時存檔，重開機接得回來**。佇列不持久化（重開機就是空的），但計時對應的是
    「客人買了多久」，不該因為伺服器重開就重算或歸零。存檔擺超過 12 小時才丟掉 ——
    那代表機器關過一整晚，昨天那一場早就散了。
  - **舞台只顯示兩件事**：最後十分鐘的角落倒數（中間那兩小時什麼都不顯示 ——
    舞台上多一個常駐的東西，就少一塊給歌詞的地方），以及最後一首唱完後的散場畫面
    （第二行回答所有人的下一個念頭：「那我排的歌呢」）。倒數排在 HUD 那一排的
    歌名與分數之間，不是絕對定位在角落 —— 角落那兩塊已經有東西，絕對定位遲早
    會疊到其中一個，而且那件事只有在包廂裡、只有在最後十分鐘才看得到。
    散場畫面的層級刻意壓在結算畫面底下：最後一首唱完時那九秒的分數是今晚
    最後一位演唱者的，不能被散場畫面蓋掉。

### 變更

- 新增 `GET /api/room` 與 `POST /api/room/{start,extend,pause,resume,stop}`；
  計時狀態跟著每一次 `STATE_UPDATE` 廣播（`state.room`），倒數在點歌台與舞台上
  才會是同一個數字。畫面上每秒跳的那一格是前端自己算的（拿到快照的剩餘秒數 ＋
  從那之後過了多久）—— 為了一個時鐘而每秒廣播一次整份狀態，是拿包廂的網路
  換一個本來就算得出來的數字。
- 伺服器多一個五秒一次的計時心跳（`room_timer_loop`），跨過提醒門檻時廣播
  `ROOM_ALERT`。提醒不能等到「下一次有人操作」才發現：包廂最安靜的時候正是
  快唱完的時候，而那正是最需要聽到「剩十分鐘」的時候。
- 時間到之後 `POST /api/queue/add` 回 **409**（`room_time_up`），插播也一樣擋 ——
  插播繞得過包廂內部的公平規則，繞不過「今晚已經結束」這件事實。這一道擋在
  點歌額度前面：「今晚結束了」蓋過「你排太多首」，講後者會讓人以為刪掉一首
  就能再點。成功時回應多一個 `room` 欄位，剩不到五分鐘時點歌會補一句
  「歡唱時間剩 4 分 12 秒，這首可能唱不完」——
  這句話講在點歌的當下有用，講在歌被停下來的那一刻就只是事後諸葛。
- 系統設定新增「⏱️ 包廂計時」七個欄位（開關、一場多久、自動開錶、兩段提醒門檻、
  時間到怎麼辦、續時一次加多久）。
- 舞台的提示（`showToast`）可以指定顯示秒數 —— 「時間到會讓這一首唱完」
  這種要看懂才有用的句子，2.6 秒不夠讀完。

## [1.13.0] - 2026-09-15

### 新增

- **每人待唱上限（點歌額度）(Per-Singer Quota)**：公平輪唱解掉的是**順序**，
  這一條解掉的是**量**。輪唱會把一個人連點的五首攤開來排，但那五首還是在
  佇列裡；包廂裡十個人、佇列裡二十首歌、其中十二首是同一個人點的，散場前
  佇列還是清不完（而且排在後面那幾首的點歌者早就走了，歌照唱，沒人上台）。
  點歌佇列標題旁邊用 **− 🎫 ＋** 調「每人最多排幾首待唱」，**0 = 不限（預設）**。
  - **算的是「待唱」，不是「今晚總共唱幾首」**。ROADMAP 把這一項掛了很久，
    掛的理由是「要先想清楚**滿了之後怎麼辦**」——「今晚最多五首」沒有好的收尾：
    有人第五首唱完、麥克風還在他手上，機器對著他說「你不能再點了」，而且是
    **永久**的，今晚剩下的三個小時都不行，除非有人去後台按重設。那不是規則，
    是懲罰。「同時最多排三首」則自己會解開：排滿了等其中一首唱完，那一格就
    空出來，不需要任何人介入，也沒有人被鎖在今晚之外。
    而且它才是包廂真正在抱怨的那件事 —— 沒有人會說「你今晚唱太多首」，
    大家會說的是「佇列整排都是你的歌」。
  - **正在唱的那一首不佔額度**。這是上面那句承諾唯一的實作依據：把已經上台的
    也算進去的話，唱完之後額度不會回來，這個功能就默默退化成永久額度。
  - **沒取暱稱的所有人共用一份額度**（身分的定義跟輪唱借同一份）。反過來寫
    （沒取名的不受限）看起來比較體貼，但它等於公告「把暱稱刪掉就無限點」，
    額度會在第一個發現這件事的人手上當場失效。照現在這個寫法，刪掉暱稱是把
    自己丟進一個**已經被別人塞滿**的桶子，嚴格比較差 —— 取暱稱才拿得到屬於
    自己的三首。額度因此是整個系統裡唯一一個「取暱稱有好處」的功能。
  - **插播（⚡）不受上限限制，但照樣佔額度**。跟輪唱對插播的處理完全一致：
    插播是現場有人按下去的決定，機器的規則不該推翻它；但它照樣算進待唱數 ——
    不算的話，插播就變成「繞過額度」的那顆按鈕，而那顆按鈕在每一張歌卡上。
  - **只擋新的，絕不回頭刪**。把上限調低（或本來關著、中途才打開）時，
    已經排進去的歌一首都不動，只是在降回上限以下之前點不了新的。
    「超過的從後面砍掉」會是這個系統最嚴重的一次背叛 —— 佇列是所有人盯著看的
    東西，有人動了一個設定、別人排好的歌就消失了，而且看不出是誰、為什麼。
  - **被擋下來時講滿三件事**（誰、現在幾首、什麼時候可以再點）：
    「⚠️ 小明 已經排了 2 首待唱（上限 2 首）。你的下一首排在第 1 位，唱完就
    空出一格。」這是整個系統裡唯一一個對使用者說「不行」的地方，少講最後一件，
    使用者的下一個動作就是再按一次。沒取暱稱的人講的是完全不同的一句話
    （他不是自己排太多，是跟所有沒取名的人共用一份額度），而且直接帶出解法。
  - 佇列上方多一行寫出現在的用量：「每人最多排 2 首待唱　小明 2/2 滿　小美 1/2」，
    **排滿的人一定被列出來**（這一行存在的理由就是「誰快滿了」）。

### 變更

- 新增 `GET /api/quota`；`pending_limit` 加入共享控制參數（`POST /api/control`），
  一支手機調，包廂裡每一台都收到同一份規則。
- `POST /api/queue/add` 額度滿時回 **409**（不是 429 —— 理由不是「按太快」，
  是「佇列現在的狀態不收這一首」），body 帶著整份結論；成功時多回一個 `quota`
  欄位（現在排了幾首、還剩幾首），畫面才講得出「還可以再排 1 首」。
- 系統設定新增「每人待唱上限」（預設 0＝不限，上限 20 首）與 🎫 區塊。
- 點歌台的提示（`showNotification`）可以指定顯示秒數並限制寬度／允許換行 ——
  「你的額度滿了，等第 1 位那首唱完」這種要看懂才有用的句子，2.5 秒剛好夠讀到
  一半然後消失，而長句在手機上原本會被推出畫面右邊。

## [1.12.0] - 2026-09-14

### 新增

- **公平輪唱（排麥輪序）(Fair Rotation)**：包廂裡最常吵的從來不是音準，是**順序**。
  一個人連點五首，其他人就得等五首；後到的朋友想唱一首，排在最後面，
  等到散場都還沒輪到。點歌佇列右上角按 **🔁 先到先唱** 切成 **🔁 輪唱中**，
  新點的歌就照「這是誰的第幾首」插進佇列 —— 大家各唱一首，唱完再輪第二首。
  - **只決定插進哪裡，不重排既有順序**。每次狀態變動就照輪次重排整個佇列在
    畫面上是災難：佇列是所有人盯著看的東西（「我那首還有幾首就到了」），
    每多一首歌整排就跳一次，而且會把剛剛有人手動拖曳的結果一起吃掉。
  - **插播（⚡）對輪序免疫**。插播是現場有人按下去的決定（生日歌、朋友要走了
    想唱最後一首），機器的規則不該推翻它。反過來，插播照樣佔掉那個人的輪次 ——
    否則它就變成「多唱一首又不影響排序」的漏洞。
  - **身分只認暱稱，不認裝置**。包廂裡的手機是傳來傳去的（「你手機借我點一首」），
    同一個人也可能一邊用點歌台、一邊用自己的手機點。沒取暱稱的所有人算**同一位**，
    所以一整間都沒取名時行為完全等於先到先唱（佇列上方會直接把這件事寫出來）。
  - **輪次算的是「今晚唱了幾首」**，不是只算佇列裡待唱的。只算待唱的話，
    唱完一首再點的人會變成「第 1 輪」而排到還沒唱過的人前面 —— 正好是這個功能
    要消滅的那件事。
  - **隔夠久沒人唱就自動歸零**，門檻與整晚打包共用同一個設定（「相隔幾小時算
    換一場」，預設 6 小時）：系統裡「一場」只能有一個定義，兩個各自可調的話會
    出現「打包算同一場、輪序算換了一場」的矛盾。時間倒退（NTP 校時）不算換場。
    換一批客人時可按佇列上方的 **重新排** 手動歸零，它**不動佇列**。
  - 點完歌直接告訴點歌的人排到哪：「🎤 已加入：🔁 排在第 2 位（小美的第 1 輪），
    插到 3 首前面」。不講的話使用者看到的是「我點的歌沒排在最後面」，
    那看起來跟壞掉一樣（然後他會再點一次，佇列裡就多一首沒人要的歌）。
  - 佇列每一列多一顆「第 N 輪」小標（只在輪唱開著時出現），
    插播則標 ⚡ 插播；上方一行寫出現在每個人「已唱幾首・待唱幾首」。

### 變更

- 佇列項目多帶一個 `priority` 欄位（插播與否），輪序靠它認出不該插到前面的歌，
  畫面也靠它標出 ⚡；`POST /api/queue/add` 的回應多一個 `placement`
  （排在第幾位、第幾輪、插到幾首前面）。
- 新增 `GET /api/rotation`、`POST /api/rotation/reset`；
  `rotation_enabled` 加入共享控制參數（`POST /api/control`），
  一支手機打開，包廂裡每一台都會收到同一份規則。
- 系統設定新增「開機就開輪唱」（預設關），設定頁新增 🔁 公平輪唱區塊。
- 「相隔幾小時算換一場」的說明改寫：它現在同時是整晚打包與輪序的「一場」定義。

### 測試

- 新增 `tests/test_rotation.py`（26 條）：輪次計算、插入位置、插播免疫、
  暱稱正規化、沒取暱稱的合桶、換場歸零與時間倒退。
- `tests/test_queue_manager.py` 加 9 條（輪唱開關、實際插入順序、已唱計數、
  placement、歸零不動佇列、換場門檻取自設定），
  `tests/test_api.py` 加 3 條（共享開關、輪序端點、歸零端點）。
- 新增 `frontend/tests/rotation-view.test.js`（17 條）：小標、人多時的省略、
  沒人取暱稱的提醒、剛點完那一句的三種情況。
- 後端 509 條 + 前端 266 條測試全綠；另外以 Playwright 在真的瀏覽器裡跑過
  開關、點歌插入順序、輪次小標與「沒有人取暱稱」的提醒。

## [1.11.0] - 2026-09-13

### 新增

- **整晚打包下載 (Night Export)**：分享連結解決的是「一個人帶走自己那一首」，
  這一版解決的是**收場時的那句話** ——「今天晚上的通通給我一份」。
  **🎙️ 錄唱回放** 最上面多一條打包列，按 **⬇️ 打包下載** 就把一整場包成
  一個 zip；旁邊的下拉選單可以只要某一位唱的那幾首。
  - **一場不是照日曆日期切的**。包廂的一場是「九點唱到凌晨兩點半」，
    照日期切會把它剖成兩半，而且唱到最嗨的後半會被標成「隔天」。
    改成照**空檔**切：相鄰兩次演唱相隔超過 6 小時（可在設定頁調）才算換一場。
    反過來也對 —— 同一天下午一輪、晚上一輪是兩桌不同的客人，日期法分不出來。
    畫面上跨午夜的場次會寫成 `今晚 21:05 – 隔天 02:30`，「隔天」那兩個字
    不寫的話會被讀成時間打反了。
  - **邊打包邊送，不先在磁碟上生一份**。一場 200 MB 的話先落檔就要另外佔
    200 MB，而錄音配額存在的理由正是「這顆磁碟會被塞爆」——
    打包下載不該是那個把磁碟塞爆的人。伺服器端全程只有幾百 KB 在記憶體裡。
  - 因此**刻意沒有 `Content-Length`**（進度條不顯示百分比，畫面上有寫）：
    精確長度要讀完才知道，先算一次再串流的話，中間只要有一筆被配額擠掉，
    送出的位元組就對不上宣告的長度，瀏覽器會把整包當成**下載失敗**。
  - **音檔一律 stored（不壓縮）**：錄音是 Opus/AAC，再 deflate 一次省不到 1%，
    換來的是把一整晚重壓一遍的 CPU —— 而那顆 CPU 正在放歌、算音準、
    跑下一首的人聲分離。只有那份純文字的曲目清單用 deflate。
  - **zip 裡的檔名帶序號與時間**（`01 21-05 月亮代表我的心 - 阿明.webm`）：
    車機、隨身碟播放器與檔案總管都是照檔名排序的，沒有序號的話「今晚唱的順序」
    在解開之後就找不回來了。Windows 的非法字元與結尾的點／空白一併處理，
    長度照 **UTF-8 位元組**裁（中文一個字 3 bytes，照字數裁會裁出存不進去的名字），
    同名的（同一首唱兩次）自動讓開成 `(2)`，不互相覆蓋。
  - **zip 裡附一份 `曲目.txt`**，而且是**最後一個項目**：它是打包完才生出來的，
    所以講得出「哪幾首沒包進來」（打包當下剛好被刪或被配額擠掉的那幾首）。
    先寫的話它會說謊，使用者會以為是解壓縮壞掉。
  - **打包途中某一首不見了就跳過並記一筆**，不是整包放棄：少一首加一行說明，
    比一個下載到 80% 斷掉的檔案有用得多。
  - **同時只打一包**（一包幾百 MB，而那條網路正是舞台端串影片與 WebSocket
    在走的）；第二個人按下去得到的是一句「正在打包另一份」，等一下再按同一個
    網址就會成功。位子用**有期限的租約**放著 —— 按了下載又立刻關掉分頁時，
    串流有可能從來沒被跑過，沒有租約期限的話那個位子就永遠空不出來。
  - **整晚打包刻意不跟著分享 token 走**：一個 zip 是整場所有人的聲音，
    那不是掃 QR 的人該拿得到的東西。這條路只在點歌台（包廂內網）出得來。
  - **整包不轉 MP3**：一整晚三十首要轉三十次，等於拿正在放歌的 CPU 去換檔案。
    要 MP3 的話在清單上按那一首的 🎧 MP3（曲目清單裡也寫了這句）。
- 新 API：`GET /api/recordings/sessions`（有哪幾場、各幾首多大、是不是正在打包）、
  `GET /api/recordings/sessions/{key}/zip?singer=`（串流下載，`singer` 可選）。
- 新設定（⚙️ 系統設定 → 📦 整晚打包）：`recording_session_gap_hours`
  （相隔幾小時算換一場，預設 6）。
- 新模組：`backend/services/night_export.py`（場次切割、檔名清洗、串流打包、
  打包名額）與 `frontend/js/night-export.js`（畫面上那一行字）。

## [1.10.0] - 2026-09-13

### 新增

- **錄音轉 MP3 (MP3 Transcoding)**：上一版讓人把那一次演唱帶走，這一版讓它
  **在帶去的地方打得開**。錄音是瀏覽器的 MediaRecorder 錄的（Chrome/Firefox 給
  webm/Opus、Safari 給 mp4/AAC），在包廂裡播沒問題，但錄音真正的去處是
  車機的 USB、長輩的舊手機、傳過去給對方直接點開 —— 那些地方多半只認 MP3。
  現在 **🎙️ 錄唱回放** 的每一列多一顆 **🎧 MP3**，分享頁上也有一顆
  **🎧 下載 MP3（車機／舊手機）**。
  - **按下去才轉，不是錄完就轉**。一個晚上錄三十首，會被帶走的大概三首；
    錄完就轉等於拿正在放歌、算音準、跑人聲分離的那顆 CPU 去換沒人要的檔案。
    轉好的留著當快取，第二次按是秒回。
  - **轉出來的 MP3 是可丟的快取，永遠不會擠掉任何一次演唱**。它有自己的上限
    （錄音配額的 1/4，額外佔用，設定頁與清單上都寫出來），滿了從**最久沒用到**
    的那一份開始刪。反過來做（讓 MP3 吃錄音配額）的後果是「有人按了一下下載，
    最舊的那一次演唱就沒了」—— 演唱刪掉就沒有，MP3 再轉一次就有。
  - **LRU 用遞增序號而不是時間戳**：同一秒內連按兩次分不出先後，而且時間會
    倒退（NTP 校時），剛用過的那一份會突然變成「最久沒碰的」而被刪掉。
  - **同一筆同時只轉一次、整台機器同時只轉一個**。前者擋的是「按了沒反應
    所以再按一次」，後者擋的是一桌人同時掃 QR 下載 —— 唱歌永遠比轉檔重要，
    寧可讓第五個人多等十秒。排不進去時給一句「現在忙」，不讓瀏覽器一直轉圈圈。
  - **ffmpeg 不在就把按鈕收起來，而不是讓人按下去才失敗**。而且分得出兩種
    不能：設定頁關掉了（去打開它）、機器上沒有 ffmpeg 或那個 ffmpeg 沒有
    libmp3lame（去裝一個完整版）—— 使用者要做的事完全不同。
    裝好之後按「重新偵測」即可，不必重開伺服器。
  - **轉檔中的檔案寫 `.part` 再原子換檔**：逾時或失敗留下的半個檔案如果被當成
    MP3 送出去，使用者聽到的是「播到一半斷掉」，他的結論會是那一次錄音壞了。
  - 下載走 fetch 再自己觸發，按鈕當場變成「⏳ 轉檔中…」：第一次要等伺服器
    真的跑一次 ffmpeg，`<a download>` 在那幾秒裡看起來完全沒反應。
  - ID3 標籤帶歌名與演唱者（車機螢幕上顯示的就是這個），取樣率固定 44.1kHz
    （48kHz 的 MP3 合法，但老車機挑食）。
- 新 API：`GET /api/recordings/{id}/audio?format=mp3`、
  `GET /api/share/{token}/audio?format=mp3`（分享頁的 MP3 鈕）、
  `DELETE /api/recordings/mp3`（清掉轉好的 MP3，錄音一個都不動）、
  `POST /api/recordings/mp3/recheck`（重新偵測 ffmpeg）。
  `GET /api/recordings` 多回一個 `mp3` 能力欄位與 `mp3_count` / `mp3_bytes` 用量。
- 新設定（⚙️ 系統設定 → 🎧 錄音轉 MP3）：`recording_mp3_enabled`、
  `recording_mp3_bitrate`。
- 新模組：`backend/services/transcoder.py`（探測、指令組裝、原子換檔、兩道關卡）。

### 修正

- 分享連結的「下載幾次就失效」不再被**失敗的**轉檔扣掉次數：計次移到真的
  拿到檔案之後 —— 否則一個沒成功的動作就把當事人的連結燒掉了。
- `-f mp3` 明確指定輸出格式：ffmpeg 是看副檔名決定 muxer 的，而原子換檔寫的是
  `xxx.mp3.part`，不講格式就直接 `Error initializing the muxer ... Invalid argument`。
  （單元測試用假的 ffmpeg 測不到這一條，是真的跑一次才發現的，現在有測試釘著。）

## [1.9.0] - 2026-09-13

### 新增

- **錄音分享 (Recording Share Links)**：唱完那一句「傳給我」在這一版結案。
  在 **🎙️ 錄唱回放** 的每一列按 **🔗 分享**，產生一個**有時效**的連結與 QR，
  當事人用手機掃走就能聽、能下載 —— 不必再擠在包廂那台機器前面聽回放，
  也不必等配額把那一次擠掉之後才後悔沒存。
  - **不是「開啟一次就作廢」的一次性連結**。名字誘人，實作上會害到當事人自己：
    播一個音檔不是一次請求（拖進度條會發 Range 請求、Safari 會為同一個檔案
    再發一次、手機切回前景也可能重連），第一個請求就把連結燒掉的話，
    使用者會看到歌播到一半死掉而且重整也救不回來。
    所以這裡的「一次性」是**時效性**：預設 24 小時後自動失效、隨時可撤銷，
    另外可設「下載幾次就失效」——但**只算明確的下載**（按下載鈕），不算播放。
  - **沒有「永不過期」這個選項**。錄到的是包廂裡所有人的聲音，一個永遠有效的
    公開連結是事後想收也收不回來的東西。上限 30 天，換掉那個選項是划算的交換。
  - **連結會跟著錄音一起死**：錄音被刪除、或被配額無聲擠掉，舊連結立刻打不開，
    而且講的是「**這一次的錄音已經不在了**」而不是「連結不存在」——
    後者會被理解成網址打錯，於是掃過 QR 的人會一直重掃。
  - **失效的四種原因分開講**（過期／撤銷／次數用完／錄音不在了）。
    全部回一句「無效」的話，拿到連結的人不知道該不該叫原點歌的人重發一個。
  - **分享頁是獨立的一頁**（`/share/<token>`），手機打開只有一張卡片：
    歌名、誰唱的、分數、播放器、下載鈕、還剩多久。刻意不載入點歌台那 1500 行
    後台樣式 —— 為了一張卡片在行動網路上多等兩秒是不值得的。
    歌名與演唱者一律用 `textContent` 塞（那是從 YouTube 抓回來的字串，
    嵌進 HTML 等於把別人取的標題當程式碼跑）。
  - 連結指向這台機器的網址，所以**預設只在同一個網路裡打得開**；
    要讓人帶回家聽，設 `KARATUBE_PUBLIC_HOST` 指到對外位址即可（見 DEPLOYMENT.md）。
    點歌台的分享視窗與分享頁的失敗畫面都把這件事講出來 ——
    連不上時最常見的原因不是系統壞了，是人已經離開那個網路。
- 新 API：`POST /api/recordings/{id}/share`（產生或沿用連結）、
  `GET /api/recordings/{id}/shares`（含已失效的，畫面要說得出原因）、
  `DELETE /api/share/{token}`（撤銷）、`GET /api/share/{token}`（分享頁的資料，
  只給這一筆看得到的欄位）、`GET /api/share/{token}/audio`、
  `GET /api/share/{token}/qr.png`、`GET /share/{token}`（分享頁本身）。
- 新設定（⚙️ 系統設定 → 🔗 錄音分享）：`recording_share_enabled`、
  `recording_share_ttl_hours`、`recording_share_max_downloads`。
- 新模組：`backend/services/share_links.py`（連結的生命週期）、
  `frontend/share.html` + `frontend/js/share-view.js`（說法，純函式、有單元測試）
  + `frontend/js/share-page.js`（畫面接線）、`frontend/css/share.css`。

### 修正

- 分享索引（`cache/recording_shares.json`）刻意**不放在錄音資料夾裡**：
  `RecordingLibrary` 會把錄音資料夾裡「不在索引上」的檔案當成斷電留下的
  孤兒檔刪掉，放進去會在下次開機時無聲消失（所有已發出去的連結一起死）。
- `frontend/tests/script-scope.test.js` 的守備範圍加入 `share.html`：
  新的一頁同樣是用 `<script src>` 載進同一個全域作用域，重名的 `const`
  會讓後載入的那一支整支不執行。

## [1.8.0] - 2026-09-13

### 新增

- **錄唱回放 (Performance Recordings)**：分數說的是「唱得好不好」，
  錄音說的是「唱起來是什麼樣子」—— 後者才是隔天還會被打開來聽的東西。
  這一版把每一次演唱（伴奏＋麥克風的混音）錄下來，在點歌台的
  **🎙️ 錄唱回放** 分頁就地播放、下載、標記保留。
  - **預設關閉，而且是唯一不開就完全不動作的功能**：錄音錄到的是包廂裡
    所有人的聲音，預設打開等於替使用者決定要錄他們講的話。舞台端錄著的時候
    左上角常駐紅點「錄音中」—— 正在被錄音是一件要一眼看到的事。
  - **錄的是喇叭音量之前的混音**。音訊圖多了一條 `mixBus`（全部聲音先匯到
    這裡再進 `masterGain`），錄音接在它上面：有人把喇叭轉小（接電話、勸酒）
    甚至靜音，錄下來的不該跟著變小 —— 錄音要的是「這首歌唱成什麼樣子」，
    跟包廂當下開多大聲是兩件事。
  - **唱太短的不留**（預設 10 秒，可調）。前奏就被切歌、麥克風擺在桌上沒人唱的
    那種錄音一樣佔配額，而且會把真正想找的那一次擠掉。判斷用的是「**唱了多久**」
    而不是「錄了多久」，而且累計的是時間不是幀數 ——
    幀數在 30fps 的機器與 120fps 的螢幕上同一首歌會差到四倍。
  - **配額是磁碟保險絲**（預設 50 首 / 512 MB）。錄音跟歌曲快取共用同一顆磁碟，
    沒有上限的話最後失敗的會是「歌曲下載不下來」，沒有人會聯想到是錄音吃光的。
    超過時從最舊、**沒有標記保留（📌）**的那一筆開始刪；配額到九成就先提醒
    （滿了才講已經來不及，下一首唱完就有一筆被刪掉了，而那一筆很可能就是想留的）。
  - **切歌照樣留、重唱直接丟**：唱了兩分鐘才被切掉，那兩分鐘一樣是一次演唱；
    按重唱時評分已經歸零，留著音檔會配上一份對不起來的成績。
    歌暫停時錄音跟著暫停（包廂在講話，那不是這次演唱的一部分），
    長度也不把暫停的那段算進去。
  - 對唱模式錄的是同一個檔案（兩支麥克風混在一起唱的那一次），
    分數欄位掛勝出者的，跟結算畫面上那個大分數一致。
- 新 API：`GET /api/recordings`（清單＋配額用量）、`POST /api/recordings`
  （音檔走 raw body、metadata 走 query string，不必多裝 `python-multipart`）、
  `GET /api/recordings/{id}/audio`（支援 Range，進度條拖得動）、
  `POST /api/recordings/{id}/pin`、`DELETE /api/recordings/{id}`、
  `DELETE /api/recordings`（預設保留標記的那幾筆）。
- 新設定（⚙️ 系統設定 → 🎙️ 錄唱回放）：`recording_enabled`、
  `recording_max_count`、`recording_max_mb`、`recording_min_sing_seconds`。
- 新模組：`frontend/js/take-rules.js`（判斷規則，純函式，舞台與點歌台共用）、
  `frontend/js/take-recorder.js`（MediaRecorder 狀態機）、
  `backend/services/recordings.py`（落地與生命週期）。

### 修正

- **配額滿了又全被標記保留時，新錄音會被「存進去再馬上刪掉」**。
  清理時唯一能刪的就是剛存進來的那一筆 —— 回報的是存檔成功，
  使用者回頭卻找不到那一次。現在當場拒絕並說明原因（是單元測試先抓到的）。
- 錄音清單的進度條原本拖不動：`MediaRecorder` 產生的 webm 標頭裡**沒有
  Duration**（串流容器，開始錄時還不知道會錄多久），瀏覽器因此把
  `audio.duration` 當成 `Infinity`。改成第一次播放時先 seek 到一個不可能的
  時間點逼瀏覽器算出真正長度再跳回 0；清單上顯示的長度則一直是錄的時候量好、
  跟著上傳存起來的那一個，不受容器影響。

## [1.7.0] - 2026-09-12

### 新增

- **跨場次段落趨勢 (Cross-session Section Trends)**：單次的段落評分回答
  「這一次哪一段唱壞了」，但那句話每次都不一樣 —— 今天副歌掉了可能只是
  沒接上氣。這一版把每一場的段落命中率留下來，唱滿三次之後告訴你真正能拿來
  練歌的那一句：**「這首歌的副歌 2 你一向掉 13 個百分點」**。
  - **比的是形狀，不是高度**：每一場先減去**那一場自己的平均**再比較。
    直接把每次的命中率平均起來，算出來的是「這個人的嗓子好不好」——
    感冒那天每一段都掉 15 個百分點，平均下來每一段看起來都是弱點。
  - **一次不算趨勢**：要三場以上才開口，而且除了平均偏差還要求**方向一致**
    （至少三分之二的場次同方向）。平均值分不出「三次都掉一點」與
    「兩次正常、一次崩掉」，但這兩件事該講的話完全不同。
  - **曲式變了就重新累積**：歌曲重新處理後 `chorus_detector` 切出來的段落
    可能不一樣，「副歌 2」在新舊兩版指的不是同一段。所以只比對段落組成
    相同的場次，並以**最新一場**的曲式為準（否則新資料永遠進不來）。
  - **每個人分開算**：段落弱點是**這個人**的弱點。把包廂裡三個人的副歌
    混成一筆平均，算出來的是這首歌的副歌難不難唱。
  - 唱畢結算畫面多一塊跨場次趨勢（中線是自己的平均，往右是主場、往左是弱點），
    對唱模式則兩位各一句話。場次不夠時明講「再唱 N 次就能看出來」，
    不是沉默 —— 使用者才知道這塊不是壞了，是還在累積。
- 點歌台新增 **📊 我的成績** 分頁：每一列是「一首歌 × 一位演唱者」的趨勢卡，
  含主場／待加強段落、對拉長條圖、進步或退步，以及「再唱一次」。
  舞台的結算畫面只亮九秒而且唱完當下人在喘，真正會回頭看的是這裡。
- 新 API：`GET /api/scores/{song_id}/trend?singer=`（單曲趨勢）、
  `GET /api/scores/trends`（總表）。`POST /api/scores` 的回應也直接帶趨勢，
  舞台端不必再打一次。
- 前端新增 `frontend/js/trend-view.js`（趨勢的呈現邏輯，純函式）：
  舞台與點歌台載的是同一支，兩邊才不會對同一份資料講出兩種說法。

### 修正

- **舞台頁面整支腳本從 v1.4.0 起就停在 `new DuetScorer()` 那一行**（嚴重）。
  `mic-agc.js` 與 `duet-scorer.js` 都在最外層宣告了 `const SILENCE_DB`
  （還有值不一樣的 `ENV_RISE_TAU` / `ENV_FALL_TAU`）。舞台的 JS 是用
  `<script>` 載進**同一個全域作用域**的，重複宣告 `const` 會讓後載入的
  `duet-scorer.js` 在解析階段就丟 SyntaxError 而整支不執行，接著 `player.js`
  用到 `DuetScorer` 時炸掉 —— 從那一行以後的舞台程式全部沒有執行。
  現有的三道檢查全都測不到：`node --check` 一次只看一支、`node --test` 給每支
  獨立的模組作用域、後端測試不碰前端。是這一版在真的瀏覽器裡開舞台頁面才發現的。
- 新增 `frontend/tests/script-scope.test.js` 守這件事：照 HTML 實際載入的清單
  檢查同頁腳本之間沒有重複的最外層 `const`/`class`，並釘住幾支模組各自寫了
  一份的小工具（`clamp` / `approach` / `dbFromRms` / `round3`）實作必須一字不差
  —— 它們重複宣告不會壞，但後載入的那一份會默默覆蓋前面的，
  哪天有人只改其中一份，瀏覽器裡的行為就會跟單元測試不一樣。

## [1.6.0] - 2026-09-11

### 新增

- **情境背景 (Ambient Stage Background)**：沒抓到 MV 的歌不再是一片黑 ——
  黑畫面在包廂裡看起來就是「這首歌壞掉了」，而且整個房間只剩字幕在發光。
  改放會跟著音樂動的情境視覺，底圖是這首歌的封面放大模糊（抓不到就不放，
  破圖比沒圖難看）。沒有歌在播的待機狀態同樣有畫面，只是動得慢。
  - **抓到的 MV 其實是一張圖也算數（這一項真正的難處）**：YouTube 上大量
    「音樂版／歌詞版」上傳是一張靜態圖配四分鐘音訊。檔案是真的下載到了，
    所以「這首有沒有 MV」會回答「有」，舞台就放一張凍結的畫面放整首。
    改成每 1.2 秒把畫面縮成 32×18 取一次樣，連續幾筆平均亮度差都低於 2
    （真正的靜止圖只有編碼雜訊）就判定「這是一張圖」並換成情境背景。
    判定約 5 秒完成，通常還藏在片頭卡後面；真的會動的 MV 一筆取樣就結案。
  - **兩個方向都閂鎖**：判定過就不再改。唱到一半背景在 MV 與情境之間來回切，
    比兩種都不完美更糟。取樣只在影片真的在播時做 —— 暫停的畫面差值恆為 0，
    照樣取樣的話按個暫停回來就會發現 MV 被換掉了。
  - **背景不准閃**：舞台螢幕在暗房間裡佔滿整面牆，高對比的亮度變化一秒三次
    以上就是光敏性癲癇的誘發條件。所以亮度走限速器（每秒最多變 0.45），
    鼓點只推動粒子與波形這類「動態」，永遠不碰亮度。
  - **五種主題**（極光／星空／霓虹／海洋／燭火）與「自動」。自動是依歌曲
    **穩定**挑一個（FNV 雜湊），不是隨機 —— 同一首歌昨天星空今天霓虹，
    第一個反應會是「機器怎麼怪怪的」。
  - **成本是最低優先**：30fps 上限、半解析度畫布（柔邊交給 CSS 的 GPU blur）、
    分頁看不見就完全不畫，並量測每幀繪製成本自動降級。
    背景掉幀沒有人會發現，字幕與評分掉幀所有人都會。
- 系統設定新增 **🌌 情境背景** 區塊：模式（沒有 MV 時／一律使用／關閉）、
  主題、亮度上限（最低 30%，不給滑到 0 —— 那等於背景被關掉但模式還說開著）。
- 佇列項目多了 `has_video` 欄位（後端從 `metadata.json` 讀）。
  沒有它的話舞台只能先試著載 `original_video.mp4`、等 404 回來才知道沒有 MV，
  那一秒的黑畫面剛好落在第一句歌詞上。
- `AudioEngine.getMusicAnalyser()`：情境背景用的音樂頻譜分析節點，
  接在伴奏＋導唱的匯流排上，**不含麥克風與罐頭音效** ——
  背景跟著人講話與掌聲抖動，看起來不是「有反應」而是「壞了」。

### 變更

- 情境背景出場時舞台的 vignette 自動放輕（字幕區那一道照樣壓好壓滿）。
  那一層的工作是把任何一支亂七八糟的 MV 壓到字幕看得清楚；情境背景的亮度
  本來就被限速器與亮度上限壓過兩道，再照 MV 的力道壓一次會疊成一片全黑。

## [1.5.0] - 2026-09-10

### 新增

- **對唱的段落對決 (Duet Section Duel)**：對唱結算畫面除了「誰贏」，
  再亮出「哪一段是誰的主場」—— 同一段落兩位的命中率往中間對拉成一排長條圖
  （左邊 A 桃紅、右邊 B 青綠，段落標籤釘在正中間），掃一眼就知道
  副歌是誰撐起來的。上方一行比數（「小明 2 段・小美 1 段・平手 3 段」），
  再加上各自的主場段落（領先最多的那一段）。
  - **分段接唱不算勝負（這一項的全部難處）**：真正的對唱歌曲本來就是
    主歌 1 你唱、主歌 2 我唱、副歌一起唱。不分辨「兩個人都唱了這一段」與
    「這一段只有一個人唱」的話，畫面會宣布「A 以 68% 完勝 B 的 0%」——
    那不是唱得比較好，那是那一段根本不是 B 唱的，而被冤枉的人沒辦法反駁。
    所以只有唱夠的段落才進對決，其餘一律標成「由 X 主唱」並淡化，不給名次；
    沒唱的那一邊顯示「—」而不是 0%（畫成 0% 看起來像「唱了但完全沒中」）。
  - **參與量差太多也不比**：B 只在副歌最後跟了一句（40 幀）、A 唱滿整段
    （400 幀）時，兩個命中率不是同一件事 —— 跟一句的人只要那一句準就有 100%。
    參與量比例低於一半就退成「這一段主要是 A 唱的」，不並排比高低。
  - **差距不到 5 個百分點算平手**：兩個人在同一段都唱到 61% 與 59% 時
    宣布主場，是在幫他們製造爭吵而不是回饋。門檻與段落評分共用一組
    （`section-scorer.js`），兩張成績單不會互相打嘴。
  - 段落多的歌超過六段時只留真的有對決的段落，並註明「另有 N 段未列出」。
- 評分歷史多了 `duel_section` 欄位（這一位在這首歌領先最多的段落）。
  長條圖是結算畫面的現場資訊不入庫，但「這首歌的副歌一向是我的主場」
  是回頭看歷史時真正有用的一句話。點歌台與手機端的對唱結算通知也會帶上。

### 變更

- `DuetScorer.verdict()` 的回傳多了 `sections`（段落對決）。
  只有一個人唱的場合形狀一樣、只是沒有對決段落，
  舞台端就不必為兩種結算各寫一套判斷。
- 舞台計分板與段落對決的兩位演唱者顏色改由 CSS 變數 `--duet-a` / `--duet-b`
  定義一次。三個地方各寫一組色碼的話，哪天換了顏色而其中一處沒改，
  畫面就開始騙人（軌跡是桃紅、計分板卻標成青綠）。

## [1.4.0] - 2026-09-09

### 新增

- **對唱模式 (Duet Mode)**：商用點歌機的「男女對唱評分」——
  兩支麥克風各自計分，唱完的結算畫面把兩個人的成績並排比出勝負。
  點歌台的「🎤🎤 對唱模式」開關按下去所有裝置同步，兩位演唱者的暱稱
  會出現在舞台計分板、浮字與評分歷史上（舞台端按 `D` 也能一鍵開關）。
  - **串音判定（這個功能的全部難處）**：兩支麥克風在同一個房間，
    A 唱歌的時候 B 的麥克風也收得到。不處理的話 B 只要舉著麥克風站著
    就會有跟 A 差不多的分數 —— 而且畫面上完全看不出異狀（分數在跳、
    Combo 也在累計），現場沒有人有辦法發現分數是假的。
    所以每一幀都要先回答「這一幀該算誰的」：
    - **電平主導**：近距離收音的串音通常比本人小 12~20 dB，
      兩支麥克風的電平差超過門檻（預設 9 dB，設定頁可調）就只算大聲的那一位；
      差不多就兩邊都算（副歌一起唱本來就該兩個人都拿分）。
    - **Schmitt 遲滯**：進入獨佔要差 9 dB，回到「兩邊都算」只要差 4.5 dB。
      沒有遲滯的話，門檻附近每一幀都在換人，兩邊的分數會亂跳。
    - **音高分歧例外**：串音是同一個聲音的複製品，所以兩邊偵測到的音高會一樣。
      反過來說，音高差 1.5 個半音以上就代表真的有兩個人在唱 ——
      這時即使小了 10 dB（唱得比較收、或麥克風靈敏度不同）也照樣給分。
      只有在訊號夠紮實（高於底噪 12 dB）時才敢用這一條，
      否則底噪的假音高會把串音全部放進來。
  - **兩種接法都支援**：兩支 USB 麥克風（各自一個輸入裝置），
    或兩支麥克風接同一台立體聲音效介面／混音器（左聲道 = A、右聲道 = B，
    這是包廂真正的接法）。介面只給單聲道時會明確說出來 ——
    悄悄退回單聲道的話 B 麥拿到的是 A 的複製品，兩個人的分數會一模一樣。
  - **只有一個人唱就不比勝負**：對唱開著但另一支麥克風放在桌上是很常見的，
    這種時候亮出「A 勝 B」不是比賽而是誤會，直接顯示那一位的單人成績單。
  - 結算畫面會標出「有多少比例的時間被判為串音」：超過 30% 就代表
    兩支麥克風靠太近或門檻設太高，分數本身已經不公平 —— 說出來，
    使用者才不會以為自己真的輸了。
  - 舞台的音準畫布同時畫兩條軌跡（A 桃紅、B 青綠），
    第二支麥克風有自己的收音電平表與自動增益（兩個人的距離不可能一樣）。
- 新增控制參數 `duet_enabled` / `duet_name_a` / `duet_name_b`（共享狀態），
  以及系統設定 `default_duet_enabled` 與 `duet_crosstalk_margin_db`。
- 新增 `POST /api/scores/duet`：兩位的成績一起記、一起廣播。
  刻意不是「呼叫兩次 `/api/scores`」—— 分兩次寫，中間斷線就會在歷史裡
  留下一場只有一個人的對唱，而且包廂裡每支手機會跳兩則結算通知。
- 評分歷史多了「某人在某首歌的個人最佳」（`singer_bests`）。
  有名字的人「刷新紀錄」指的是刷新自己的紀錄，
  拿全機器最高分去比等於每次都跟包廂裡唱最好的那個人比，沒有意義。

### 變更

- `pitchEngine.tick()` 多了 `credit` / `reuseRms` 兩個選項：
  判定為串音的幀照樣偵測音高（判定本身需要它）但不計分、不進段落統計、
  不畫軌跡；電平已由外面量過時不重抓波形（自相關是整條迴圈裡最貴的一步）。
- 麥克風前級鏈改由工廠函式產生（`_createMicChain`），A / B 兩支共用同一份參數。
  兩邊各寫一次的話，哪天調了 A 的 de-esser 卻忘了 B，
  在包廂裡只會被講成「B 麥比較差」，沒人會想到是程式。
- 對唱模式下和聲只疊在 A 麥上：第二支麥克風本身就是第二個聲部。

### 測試

- 新增 `frontend/tests/duet-scorer.test.js`（25 條）：串音判定、遲滯的兩個方向、
  音高分歧例外與它的訊號門檻、門檻夾限、開關重置、時間統計、
  平手門檻、只有一個人唱時不比勝負、壞資料容錯。
- 新增 18 條後端測試：對唱結算的兩筆一起記（缺一邊就整筆不收）、
  每位演唱者各自的個人最佳、平手門檻與舞台端一致、
  舊版評分歷史檔（沒有 `singer_bests`）照樣讀得起來、
  對唱控制參數與暱稱長度上限、串音門檻設定的夾限。
  後端 286 條、前端 139 條全綠。

## [1.3.0] - 2026-09-08

### 新增

- **和聲 / 雙聲部 (Auto Harmony)**：商用點歌機的 HARMONY 鍵 ——
  一個人唱，喇叭出來像兩個人。系統把麥克風訊號即時移調，疊在旋律的
  三度／五度／低八度上，五種聲部一鍵切換（三度、下三、五度、八度、雙聲）。
  - **依調性照音階算移調量**，不是固定升幾個半音。C 大調的 Do 上三度是 +4，
    但 Re 的上三度是 +3 —— 固定 +4 會唱出 Fa♯（調外音），那就是走音。
    調性用 Krumhansl-Kessler 輪廓的相關分析從導唱音符估出來，
    權重看時間長度（單一長音上限 2 秒），有效音符不足 8 秒或相關低於 0.45 就不給。
  - **判不出調性自動退成低八度疊唱**：八度在任何調都不會錯，
    寧可少一點花樣，也不要有一半機率唱出調外音。
  - **沒人唱就不出聲**（否則疊出來的是移調過的呼吸聲與空調聲）；
    需要音階資訊的聲部在前奏／間奏也安靜；同一個音符不重算移調量，
    換音符至少隔 80ms，避免在音符邊界上抖動。
  - 和聲音量上限 0.85，**永遠不會蓋過主唱**，低八度再自動收一點。
  - 舞台右下角 🎶 徽章顯示聲部與判到的調性（和聲不對時唯一有用的線索）。
- **移調 DSP（AudioWorklet）**：`frontend/js/harmony-worklet.js`。
  Web Audio 沒有內建的移調節點，所以自己算 —— 可變延遲移調 ×2 條讀取頭，
  視窗 45ms、交叉淡接只佔一輪的 30%（把梳型抵消從 27% 壓到 8% 以內）。
  DSP 核心不依賴 Web Audio，CI 每次真的量一次「220Hz 升八度是不是 440Hz」。
- 新增控制參數 `harmony_enabled` / `harmony_style` / `harmony_level`（共享狀態，
  點歌台按下去所有裝置同步），以及系統設定頁的開機預設值三欄。
- 點歌台調音台新增 **🎶 和聲（雙聲部）** 區塊；「乾聲」按鈕現在也會一起關掉和聲。

### 變更

- `pitchEngine.tick()` 回傳的逐幀判定多了 `noteMidi` / `noteStart` / `userMidi`：
  和聲吃的是同一份判定，不重新偵測一次音高（兩邊算出不同結果會非常難查）。

### 測試

- 新增 `frontend/tests/harmony-planner.test.js`（27 條）：音階度數（含跨八度、
  非音階音吸附、小調）、調性判定門檻與壞資料容錯、出聲時機、音量上限、
  換歌重估調性、換風格立刻生效。
- 新增 `frontend/tests/harmony-shifter.test.js`（12 條）：實際量頻率驗證移調量、
  兩條讀取頭增益恆為 1、音量守恆、分區塊與一次處理結果一致、延遲在一個視窗內。
- 新增 4 條後端測試（和聲控制參數是共享狀態、認不得的聲部保留原值、音量夾限、
  設定頁預設值）。後端 268 條、前端 114 條全綠。

## [1.2.0] - 2026-09-08

### 新增

- **排程預處理 (Scheduled Batch Pre-processing)**：把等待挪到沒人唱歌的時候。
  晚上把整張 YouTube 播放清單（或一堆網址、關鍵字混著）貼進點歌台的
  「🌙 排程預處理」分頁，機器會在設定的時段（預設 02:00–06:00，可跨午夜、
  可設全天候）一首一首跑完整條流水線，隔天點下去全部是「⚡ 快取秒播」。
  - **有人在唱歌就讓開**：每處理完「一首」就重新確認一次現場狀況，
    客人推門進包廂點第一首歌時，機器最多再忙一首歌的時間就會把資源讓出來。
  - **一首失敗不拖垮整批**：影片下架、網路斷線只標記那一首，其餘照跑；
    事後按「🔁 重試失敗」只重跑壞掉的那幾首。
  - **已在曲庫的自動略過**，貼一張 30 首的清單會直接顯示「其實只有 3 首要跑」。
  - **重開機續跑**：任務存在 `cache/batch_jobs.json`；上次中斷在半路的那首
    退回等待重跑，使用者按過的「立即開始」也會被記住。
- 新增 API：`GET/POST/DELETE /api/batch`、`POST /api/batch/force`、
  `POST /api/batch/{job_id}/retry|cancel`，進度以 `BATCH_UPDATE` 廣播給所有裝置。
- 系統設定頁新增 **🌙 排程預處理**（開關、開工／收工時間、有人唱歌時暫停）。
- `YouTubeSearchService.expand_sources()`：把播放清單網址、單曲網址與關鍵字
  混在一起的輸入展開成歌曲清單，展不開的行回報給使用者而不是讓整批失敗。

### 變更

- FastAPI 改用 `lifespan` 管理背景工作（取代已棄用的 `on_event`），
  排程迴圈隨伺服器啟動、關機時確實收掉。
- `SongStorage` 新增 `is_song_complete()`：只看 metadata 會把處理到一半的殘留
  資料夾誤判成「已完成」，那首歌就永遠不會被補跑。

### 測試

- 新增 `tests/test_batch_scheduler.py`（27 條）：跨午夜時段、有人唱歌時的讓路、
  一首失敗不拖垮整批、只重試失敗項、立即開始的自動收回、重開機續跑與壞檔容錯。
- 新增 `frontend/tests/batch-view.test.js`（8 條）：一批上百首時畫面挑哪幾首顯示、
  時段字串、狀態燈優先度、計數摘要不顯示 0。
- 新增 6 條 `/api/batch` 端點測試。後端 264 條、前端 75 條全綠。

## [1.1.0] - 2026-09-07

### 新增

- **麥克風自動增益 (Mic AGC)**：換人唱不用重調麥克風音量 —— 機器自己把每個人的
  收音電平拉到同一個目標（預設 -18 dBFS）。前饋量測（量增益節點之前的原始訊號）、
  安靜時絕不加大（噪音閘門 -45 dBFS）、要降立刻降（0.25s）要升慢慢升（3s）、
  削峰保護（預估輸出過 -6 dBFS 就用 0.06s 壓下來）、加成上限 +9 dB
  且多人模式收到 +6 dB。舞台 🎤 徽章、音訊面板的即時收音電平表，
  以及唱畢結算的「拿近一點／拿遠一點」建議。
- 系統設定頁新增 **麥克風自動增益**（開關、目標收音電平）。
- 舞台音訊面板（按 `S`）新增 **📶 收音電平表**：太小聲偏左灰、正常青色、快削峰轉粉紅。
  麥克風沒插好、選錯裝置、靜音鍵沒開，在這裡三秒就看得出來。

### 變更

- 麥克風處理鏈多一段 `micAgcGain`，位置在 de-esser 之後、limiter **之前**
  （自動增益判斷失誤時還有 limiter 擋著），與使用者的麥克風音量各走一個節點。
- `PitchEngine` 多開放 `measureRms()` 與逐幀的 `frame.rms`：自動增益與電平表
  沿用評分心跳已經抓過的波形，不再重複抓一次。

### 測試

- 新增 `frontend/tests/mic-agc.test.js`（24 條）：安全規則（靜音不加大、不對稱曲線、
  削峰保護不是「不准再降」的地板）、遲滯收斂、幀率無關性、壞資料防護。
- 後端 232 條、前端 67 條全綠。

## [1.0.0] - 2026-09-07

第一個正式發布版。商用 KTV 點歌機的功能對照清單（見 `docs/ROADMAP.md`）全數完成。

### 新增（本次發布週期）

- **導唱音量自動 ducking**：導唱人聲跟著唱的人走 —— 唱穩了自動退到背景，
  走音或忘詞時立刻回來。不對稱曲線（慢退 1.6s / 快回 0.22s）＋ Schmitt 遲滯門檻
  ＋ 3 秒暖機 ＋ 深度上限 0.95（永遠留安全網）。舞台徽章與結算「導唱獨立度」。
- **Docker 一鍵部署**：`Dockerfile` + `docker-compose.yml`，快取與模型權重各自掛 volume，
  非 root 執行，`/api/health` 健康檢查；GPU 版只要換底層映像。
- **版本號與發布流程**：`backend/version.py` 為唯一來源，`GET /api/version` 可查，
  設定頁頁尾顯示；打 `v*` 標籤自動驗證（跑完整 CI ＋ 比對版本號）並打包 release。
- **部署設定環境變數化**：`KARATUBE_HOST` / `KARATUBE_PORT` / `KARATUBE_PUBLIC_HOST` /
  `KARATUBE_PUBLIC_PORT` / `KARATUBE_CACHE_DIR`，容器與反向代理情境下
  QR code 與「手機點歌」網址才會印對。
- 新增 [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) 安裝與部署說明。

### 這一版包含的完整功能

- YouTube / YouTube Music 隨選點歌（關鍵字或網址），處理過的歌永久快取秒播
- Demucs v4 AI 人聲伴奏分離，導唱人聲 0~100% 無段調節
- LRC 主導、聲學校正的逐字時間軸；雙行 KTV 變色字幕與倒數預備光點
- 即時音準導唱線與 JOYSOUND / DAM 風格評分（Combo、PERFECT / GREAT / GOOD）
- 唱畢結算：總分、SSS~C 等級、音準率、最大 Combo、個人最佳、擊敗比例、段落評分
- 練唱模式：A-B 區段循環、自動副歌偵測、段落跳轉
- 升降 Key（±6 半音）、男調/女調一鍵切換、殘響/回音四參數與三種效果風格
- EBU R128 自動音量平衡（ITU-R BS.1770-4 K 加權 + 雙重閘門）
- 曲庫分類瀏覽（語言別／歌手自動判定）、新歌榜、推薦歌單、熱門排行、我的最愛、已唱歷史
- 雙螢幕輸出（點歌台 + 舞台）、手機掃碼點歌、多人包廂暱稱
- 快取管理與錯誤歌曲重新處理、系統設定頁
- CI：後端 `compileall` + `ruff` + 231 條 pytest，前端 `node --check` + 43 條 `node --test`

[1.1.0]: https://github.com/dofliu/KaraTube/releases/tag/v1.1.0
[1.0.0]: https://github.com/dofliu/KaraTube/releases/tag/v1.0.0
