# 更新日誌 (Changelog)

本檔記錄每個發布版本的變更。格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，
版本號遵循 [語意化版本](https://semver.org/lang/zh-TW/)。

逐次迭代的開發細節（為什麼這樣做、踩到什麼）記在 [docs/PROGRESS_LOG.md](docs/PROGRESS_LOG.md)。

> 版本號的唯一真相來源是 `backend/version.py`。發布流程會檢查 git 標籤、
> `backend/version.py` 與本檔最上面那一筆三者一致，對不上就不給發。

## [1.3.0] - 2026-09-08

### 新增

- **和聲 / 雙聲部 (Auto Harmony)**：商用點歌機的 HARMONY 鍵 ——
  一個人唱，喇叭出來像兩個人。系統把麥克風訊號即時移調，疊在旋律的
  三度／五度／低八度上，五種聲部一鍵切換（三度、下三、五度、八度、雙聲）。
  - **依調性照音階算移調量**，不是固定升幾個半音。C 大調的 Do 上三度是 +4，
    但 Re 的上三度是 +3 —— 固定 +4 會唱出 Fa♯（調外音），那就是走音。
    調性用 Krumhansl-Kessler 輪廓的相關分析從導唱音符估出來，
    權重看時間長度（單一長音上限 2 秒），有效音符不足 8 秒或相關低於 0.45 就不給。
  - **判不出調性自動退成低八度疊唱**：八度在任何調都不會錯，
    寧可少一點花樣，也不要有一半機率唱出調外音。
  - **沒人唱就不出聲**（否則疊出來的是移調過的呼吸聲與空調聲）；
    需要音階資訊的聲部在前奏／間奏也安靜；同一個音符不重算移調量，
    換音符至少隔 80ms，避免在音符邊界上抖動。
  - 和聲音量上限 0.85，**永遠不會蓋過主唱**，低八度再自動收一點。
  - 舞台右下角 🎶 徽章顯示聲部與判到的調性（和聲不對時唯一有用的線索）。
- **移調 DSP（AudioWorklet）**：`frontend/js/harmony-worklet.js`。
  Web Audio 沒有內建的移調節點，所以自己算 —— 可變延遲移調 ×2 條讀取頭，
  視窗 45ms、交叉淡接只佔一輪的 30%（把梳型抵消從 27% 壓到 8% 以內）。
  DSP 核心不依賴 Web Audio，CI 每次真的量一次「220Hz 升八度是不是 440Hz」。
- 新增控制參數 `harmony_enabled` / `harmony_style` / `harmony_level`（共享狀態，
  點歌台按下去所有裝置同步），以及系統設定頁的開機預設值三欄。
- 點歌台調音台新增 **🎶 和聲（雙聲部）** 區塊；「乾聲」按鈕現在也會一起關掉和聲。

### 變更

- `pitchEngine.tick()` 回傳的逐幀判定多了 `noteMidi` / `noteStart` / `userMidi`：
  和聲吃的是同一份判定，不重新偵測一次音高（兩邊算出不同結果會非常難查）。

### 測試

- 新增 `frontend/tests/harmony-planner.test.js`（27 條）：音階度數（含跨八度、
  非音階音吸附、小調）、調性判定門檻與壞資料容錯、出聲時機、音量上限、
  換歌重估調性、換風格立刻生效。
- 新增 `frontend/tests/harmony-shifter.test.js`（12 條）：實際量頻率驗證移調量、
  兩條讀取頭增益恆為 1、音量守恆、分區塊與一次處理結果一致、延遲在一個視窗內。
- 新增 4 條後端測試（和聲控制參數是共享狀態、認不得的聲部保留原值、音量夾限、
  設定頁預設值）。後端 268 條、前端 114 條全綠。

## [1.2.0] - 2026-09-08

### 新增

- **排程預處理 (Scheduled Batch Pre-processing)**：把等待挪到沒人唱歌的時候。
  晚上把整張 YouTube 播放清單（或一堆網址、關鍵字混著）貼進點歌台的
  「🌙 排程預處理」分頁，機器會在設定的時段（預設 02:00–06:00，可跨午夜、
  可設全天候）一首一首跑完整條流水線，隔天點下去全部是「⚡ 快取秒播」。
  - **有人在唱歌就讓開**：每處理完「一首」就重新確認一次現場狀況，
    客人推門進包廂點第一首歌時，機器最多再忙一首歌的時間就會把資源讓出來。
  - **一首失敗不拖垮整批**：影片下架、網路斷線只標記那一首，其餘照跑；
    事後按「🔁 重試失敗」只重跑壞掉的那幾首。
  - **已在曲庫的自動略過**，貼一張 30 首的清單會直接顯示「其實只有 3 首要跑」。
  - **重開機續跑**：任務存在 `cache/batch_jobs.json`；上次中斷在半路的那首
    退回等待重跑，使用者按過的「立即開始」也會被記住。
- 新增 API：`GET/POST/DELETE /api/batch`、`POST /api/batch/force`、
  `POST /api/batch/{job_id}/retry|cancel`，進度以 `BATCH_UPDATE` 廣播給所有裝置。
- 系統設定頁新增 **🌙 排程預處理**（開關、開工／收工時間、有人唱歌時暫停）。
- `YouTubeSearchService.expand_sources()`：把播放清單網址、單曲網址與關鍵字
  混在一起的輸入展開成歌曲清單，展不開的行回報給使用者而不是讓整批失敗。

### 變更

- FastAPI 改用 `lifespan` 管理背景工作（取代已棄用的 `on_event`），
  排程迴圈隨伺服器啟動、關機時確實收掉。
- `SongStorage` 新增 `is_song_complete()`：只看 metadata 會把處理到一半的殘留
  資料夾誤判成「已完成」，那首歌就永遠不會被補跑。

### 測試

- 新增 `tests/test_batch_scheduler.py`（27 條）：跨午夜時段、有人唱歌時的讓路、
  一首失敗不拖垮整批、只重試失敗項、立即開始的自動收回、重開機續跑與壞檔容錯。
- 新增 `frontend/tests/batch-view.test.js`（8 條）：一批上百首時畫面挑哪幾首顯示、
  時段字串、狀態燈優先度、計數摘要不顯示 0。
- 新增 6 條 `/api/batch` 端點測試。後端 264 條、前端 75 條全綠。

## [1.1.0] - 2026-09-07

### 新增

- **麥克風自動增益 (Mic AGC)**：換人唱不用重調麥克風音量 —— 機器自己把每個人的
  收音電平拉到同一個目標（預設 -18 dBFS）。前饋量測（量增益節點之前的原始訊號）、
  安靜時絕不加大（噪音閘門 -45 dBFS）、要降立刻降（0.25s）要升慢慢升（3s）、
  削峰保護（預估輸出過 -6 dBFS 就用 0.06s 壓下來）、加成上限 +9 dB
  且多人模式收到 +6 dB。舞台 🎤 徽章、音訊面板的即時收音電平表，
  以及唱畢結算的「拿近一點／拿遠一點」建議。
- 系統設定頁新增 **麥克風自動增益**（開關、目標收音電平）。
- 舞台音訊面板（按 `S`）新增 **📶 收音電平表**：太小聲偏左灰、正常青色、快削峰轉粉紅。
  麥克風沒插好、選錯裝置、靜音鍵沒開，在這裡三秒就看得出來。

### 變更

- 麥克風處理鏈多一段 `micAgcGain`，位置在 de-esser 之後、limiter **之前**
  （自動增益判斷失誤時還有 limiter 擋著），與使用者的麥克風音量各走一個節點。
- `PitchEngine` 多開放 `measureRms()` 與逐幀的 `frame.rms`：自動增益與電平表
  沿用評分心跳已經抓過的波形，不再重複抓一次。

### 測試

- 新增 `frontend/tests/mic-agc.test.js`（24 條）：安全規則（靜音不加大、不對稱曲線、
  削峰保護不是「不准再降」的地板）、遲滯收斂、幀率無關性、壞資料防護。
- 後端 232 條、前端 67 條全綠。

## [1.0.0] - 2026-09-07

第一個正式發布版。商用 KTV 點歌機的功能對照清單（見 `docs/ROADMAP.md`）全數完成。

### 新增（本次發布週期）

- **導唱音量自動 ducking**：導唱人聲跟著唱的人走 —— 唱穩了自動退到背景，
  走音或忘詞時立刻回來。不對稱曲線（慢退 1.6s / 快回 0.22s）＋ Schmitt 遲滯門檻
  ＋ 3 秒暖機 ＋ 深度上限 0.95（永遠留安全網）。舞台徽章與結算「導唱獨立度」。
- **Docker 一鍵部署**：`Dockerfile` + `docker-compose.yml`，快取與模型權重各自掛 volume，
  非 root 執行，`/api/health` 健康檢查；GPU 版只要換底層映像。
- **版本號與發布流程**：`backend/version.py` 為唯一來源，`GET /api/version` 可查，
  設定頁頁尾顯示；打 `v*` 標籤自動驗證（跑完整 CI ＋ 比對版本號）並打包 release。
- **部署設定環境變數化**：`KARATUBE_HOST` / `KARATUBE_PORT` / `KARATUBE_PUBLIC_HOST` /
  `KARATUBE_PUBLIC_PORT` / `KARATUBE_CACHE_DIR`，容器與反向代理情境下
  QR code 與「手機點歌」網址才會印對。
- 新增 [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) 安裝與部署說明。

### 這一版包含的完整功能

- YouTube / YouTube Music 隨選點歌（關鍵字或網址），處理過的歌永久快取秒播
- Demucs v4 AI 人聲伴奏分離，導唱人聲 0~100% 無段調節
- LRC 主導、聲學校正的逐字時間軸；雙行 KTV 變色字幕與倒數預備光點
- 即時音準導唱線與 JOYSOUND / DAM 風格評分（Combo、PERFECT / GREAT / GOOD）
- 唱畢結算：總分、SSS~C 等級、音準率、最大 Combo、個人最佳、擊敗比例、段落評分
- 練唱模式：A-B 區段循環、自動副歌偵測、段落跳轉
- 升降 Key（±6 半音）、男調/女調一鍵切換、殘響/回音四參數與三種效果風格
- EBU R128 自動音量平衡（ITU-R BS.1770-4 K 加權 + 雙重閘門）
- 曲庫分類瀏覽（語言別／歌手自動判定）、新歌榜、推薦歌單、熱門排行、我的最愛、已唱歷史
- 雙螢幕輸出（點歌台 + 舞台）、手機掃碼點歌、多人包廂暱稱
- 快取管理與錯誤歌曲重新處理、系統設定頁
- CI：後端 `compileall` + `ruff` + 231 條 pytest，前端 `node --check` + 43 條 `node --test`

[1.1.0]: https://github.com/dofliu/KaraTube/releases/tag/v1.1.0
[1.0.0]: https://github.com/dofliu/KaraTube/releases/tag/v1.0.0
